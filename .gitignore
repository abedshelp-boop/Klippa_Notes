node_modules/
dist/
.env
*.pyc
__pycache__/
python-service/venv/
python-service/.venv/
klippa.db
pending/
*.wav
*.mp3
*.egg-info/

# Local Claude Code session/permission state — per-machine, not shareable.
.claude/

# Wake-word Picovoice bundle downloads. The extracted .ppn is committed
# under python-service/models/ — the zip itself is redundant.
*.zip

# electron-builder NSIS installer output (Deen Notes Setup *.exe etc.)
release/

# Bundled .env (generated by scripts/copy-env.js at build time, contains API keys)
python-service/.env

# Test artifacts (vitest + pytest output dirs)
coverage/
.pytest_cache/
.vitest-cache/
.superpowers/
